{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Interest Hub application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user (matches auth uid)."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        }
      },
      "required": ["id", "email", "createdAt"]
    },
    "Interest": {
      "title": "Interest",
      "type": "object",
      "description": "Represents a user's interest.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the interest."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the interest was created.",
          "format": "date-time"
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this interest."
        }
      },
      "required": ["name", "createdAt", "userId"]
    },
    "Topic": {
      "title": "Topic",
      "type": "object",
      "description": "Represents a topic within an interest.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the topic."
        },
        "interestId": {
          "type": "string",
          "description": "The ID of the parent interest."
        },
        "imageId": {
          "type": "string",
          "description": "ID for the placeholder image."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the topic was created.",
          "format": "date-time"
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this topic."
        }
      },
      "required": ["name", "interestId", "imageId", "createdAt", "userId"]
    },
    "Goal": {
      "title": "Goal",
      "type": "object",
      "description": "Represents a goal within a topic.",
      "properties": {
        "title": {
          "type": "string",
          "description": "Name of the goal."
        },
        "topicId": {
          "type": "string",
          "description": "The ID of the parent topic."
        },
        "dueDate": {
          "type": "string",
          "description": "The date and time when the goal is due.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the goal.",
          "enum": ["chưa bắt đầu", "đang làm", "hoàn thành", "thất bại"]
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the goal was created.",
          "format": "date-time"
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this goal."
        }
      },
      "required": ["title", "topicId", "status", "createdAt", "userId"]
    },
    "Task": {
      "title": "Task",
      "type": "object",
      "description": "Represents a task associated with a goal or a topic.",
      "properties": {
        "text": {
          "type": "string",
          "description": "Description of the task."
        },
        "goalId": {
          "type": "string",
          "description": "The ID of the parent goal. Optional."
        },
        "topicId": {
          "type": "string",
          "description": "The ID of the parent topic if not associated with a goal. Optional."
        },
        "scheduledDate": {
          "type": "string",
          "description": "The date and time when the task is scheduled.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the task.",
          "enum": ["chưa bắt đầu", "đang làm", "hoàn thành", "thất bại"]
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the task was created.",
          "format": "date-time"
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this task."
        }
      },
      "required": ["text", "status", "createdAt", "userId"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user profile information. The document ID is the user's UID."
    },
    "/interests/{interestId}": {
      "schema": { "$ref": "#/entities/Interest" },
      "description": "Top-level collection for interests. Contains a userId field for security rules."
    },
    "/topics/{topicId}": {
      "schema": { "$ref": "#/entities/Topic" },
      "description": "Top-level collection for topics. Contains a userId field for security rules."
    },
    "/goals/{goalId}": {
      "schema": { "$ref": "#/entities/Goal" },
      "description": "Top-level collection for goals. Contains a userId field for security rules."
    },
    "/tasks/{taskId}": {
      "schema": { "$ref": "#/entities/Task" },
      "description": "Top-level collection for tasks. Contains a userId field for security rules."
    }
  }
}
